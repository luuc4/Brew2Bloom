# Brew2Bloom

**Brew. Grow. Repeat.** — Urban gardening kits made from upcycled coffee grounds.

## Run locally

It's a static single-page site. Open `index.html` in a browser, or serve it:

```bash
python3 -m http.server 8000
# then open http://localhost:8000
```

## Files

- `index.html` — markup
- `styles.css` — all styles, mobile-first
- `script.js` — animated impact counters

## Design tokens

| Token | Hex | Use |
|---|---|---|
| Espresso Black | `#1A1A1A` | Type, contrast surfaces |
| Raw Earth | `#B5651D` | Substrate accent (warm terracotta) |
| Minty Leaf | `#B4D3B2` | Highlights, tags |
| Recycled Paper | `#F5F5DC` | Background |

Headlines: Space Grotesk · Body: Lora.
